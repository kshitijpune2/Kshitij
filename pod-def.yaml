apiVersion: apps/v1
kind: Deployment
metadata:
  name: baap-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: baap-ui
  template:
    metadata:
      labels:
        app: baap-ui
    spec:
      containers:
      - image: nginx
        name: baap-ui
        ports:
        - containerPort: 80
